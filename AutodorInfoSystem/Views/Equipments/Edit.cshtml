@model AutodorInfoSystem.Models.Equipment

@{
    ViewData["Title"] = "Редактирование спецтехники";
}

<h1 class="mb-4">Редактирование спецтехники</h1>

<div class="card p-4">
    <div class="row">
        <div class="col">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input asp-for="IdEquipment" hidden/>
                <div class="form-group">
                    <label asp-for="Name" class="control-label">Название</label>
                    <input asp-for="Name" class="form-control" id="equipmentName" onkeyup="fetchSimilarNames()" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                    <ul id="suggestions" class="list-group mt-2" style="display:none;"></ul> <!-- Список для автозаполнения -->
                </div>
                <div class="form-group">
                    <label asp-for="Price" class="control-label">Цена</label>
                    <input asp-for="Price" class="form-control" id="equipmentPrice" />
                    <span asp-validation-for="Price" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Quantity" class="control-label">Количество</label>
                    <input asp-for="Quantity" class="form-control" />
                    <span asp-validation-for="Quantity" class="text-danger"></span>
                </div>
                <input type="hidden" name="IdTask" value="@ViewBag.IdTask" />
                <div class="form-group">
                    <button class="btn btn-primary" type="submit">
                        <i class="fas fa-check"></i>
                        Сохранить
                    </button>
                </div>
                <div class="mt-3">
                    <a asp-controller="Tasks" asp-action="Details" asp-route-id="@ViewBag.IdTask" class="btn btn-outline-primary">
                        <i class="fas fa-arrow-left"></i>
                        Назад
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/create-and-edit-components.js"></script>
}

